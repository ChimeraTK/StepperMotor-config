# if${ACCELERATOR} == DEVEL

SERVERNAME=steppermotor_${STATION}_server
SVR_LOCATION=${HOSTNAME:uppercase}.${STATION}._SVR
FACILITY=TEST.ACTUATOR
DEVICE=MOTOR
WATCHDOG_ADDRESS=TEST.SYSTEM/${HOSTNAME:uppercase}.WATCH/SVR.STEPPERMOTOR_${STATION}
